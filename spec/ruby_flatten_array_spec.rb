require "spec_helper"

RSpec.describe RubyFlattenArray do
  it "has a version number" do
    expect(RubyFlattenArray::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
